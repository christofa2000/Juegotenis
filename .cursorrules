# .cursorrules
# ──────────────────────────────────────────────────────────────
# 🧠 Cursor Development Rules — Landing Tenis (Christian, 2025)
# Stack: Next.js 16 • TypeScript • Tailwind v4
# Objetivo: Landing page tenis pixel-perfect, rápida (CWV), accesible, SEO-ready.
# ──────────────────────────────────────────────────────────────

## 🧭 Filosofía General
- Priorizar simplicidad: landing ≠ dashboard.
- Pixel-perfect respecto al diseño, pero con criterio responsive.
- Componentes chicos y reutilizables; evitar páginas con lógica.
- Mobile-first y accesibilidad desde el inicio.
- Performance y SEO son features.

---

## 🧩 Estructura del Proyecto (App Router)
app/
  (marketing)/
    page.tsx
    layout.tsx
  api/ (solo si hay contacto)
components/
  marketing/
    hero/
    sections/
    ui/
lib/
  config/
  seo/
  utils/
styles/
  globals.css
public/
  images/
  fonts/

Reglas:
- Imports absolutos: @/components/...
- Nada de “mega-components”: dividir por sección.
- Todo componente UI reutilizable va a components/marketing/ui.

---

## ⚛️ Reglas Next.js / React
- Server Components por defecto.
- `use client` SOLO para:
  - Menú mobile (drawer)
  - Formularios (contacto)
  - Animaciones (si se usa Framer Motion)
- Implementar:
  - app/(marketing)/loading.tsx (si hay lazy)
  - app/(marketing)/not-found.tsx
  - app/(marketing)/error.tsx (si hay fetch/contacto)
- `generateMetadata` + metadata por defecto en lib/seo.

---

## 🔤 Fonts y Assets
- Fuentes locales SIEMPRE con `next/font/local`.
- En `/public/fonts/` guardar:
  - Lexend-VariableFont_wght.ttf (si aplica)
- En `/public/images/` guardar:
  - hero.webp (preferido) + fallback jpg si hace falta

Imágenes:
- `next/image` para todo lo que sea contenido.
- Background decorativo: CSS + overlay + `prefers-reduced-motion`.

---

## 🎨 Tailwind v4 / Tokens
- No hardcodear colores en JSX.
- Definir tokens en `globals.css` (OKLCH recomendado):
  - brand (tenis)
  - surface / muted / text
  - border / ring

Ejemplo:
@layer tokens {
  --brand-500: oklch(70% 0.1 180);
  --surface-0: oklch(99% 0 0);
  --text-950: oklch(18% 0.02 270);
}

- Dark mode: class="dark" + variables.
- Tipografía: usar variable CSS `--font-lexend`.

---

## 🧱 TypeScript
- `strict: true` obligatorio.
- Prohibido `any`.
- Props tipadas con interfaces.
- Reutilizar tipos en /types si aparecen varios.

---

## 🧠 Secciones típicas (Landing Tenis)
El layout se arma en este orden (si el diseño lo indica):
- Header (logo + nav + CTA)
- Hero (headline + sub + CTA + imagen)
- Beneficios (3–6 cards)
- Servicios/Clases (grilla)
- Testimonios (opcional)
- Pricing (opcional)
- FAQ (acordeón)
- Contacto (form + datos)
- Footer (links + redes)

Reglas:
- Cada sección = 1 componente en components/marketing/sections/*
- UI shared (Button, Badge, Card) en components/marketing/ui/*

---

## ♿ Accesibilidad
- Semántica: <header>, <main>, <section>, <footer>.
- Botones reales para acciones, links reales para navegación.
- Focus visible siempre.
- Contraste mínimo 4.5:1 (ideal AAA).
- Respetar `prefers-reduced-motion`.

---

## 🚀 Performance (CWV)
- Hero image: `priority`.
- Evitar librerías pesadas si no suman.
- Animaciones sutiles; nada de scroll-jank.
- `next/image` + formatos WebP/AVIF.
- Minimizar JS en cliente: menú mobile simple.

---

## 🔍 SEO
- `generateMetadata` por route.
- OpenGraph + Twitter cards.
- JSON-LD si corresponde (LocalBusiness/Organization).
- Headings correctos: 1 h1 por página.
- Alt text descriptivo (no “imagen1”).

---

## 🧰 Estilo / Código
- Indentación: tabs
- Strings: 'single quotes'
- Sin `;` (salvo casos ambiguos)
- Máx 100 chars por línea
- Sin console.log en prod
- Nombres:
  - handlers: handleSubmit
  - booleans: isOpen / hasScrolled
  - components: PascalCase
- Cada archivo generado por IA:
  // generated with Cursor — reviewed by Christian Oscar Papa

---

## 🧪 Testing (solo si el proyecto lo pide)
- Unit mínimo para helpers (lib/utils).
- E2E (Playwright) para:
  - navegación
  - submit de contacto (si existe)

---

## ✅ Definition of Done
Antes de merge:
- Lighthouse: Performance > 90
- A11y: > 95
- No TS errors / no lint errors
- Responsive OK: 360 / 768 / 1024 / 1440
- Imágenes optimizadas + fonts locales
